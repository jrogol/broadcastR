library(rvest)
library(dplyr)


# GENERICS ----------------------------------------------------------------
# generic function with switch

getRoster <- function(team,url,source) {
  # Match args - source
  
  # switch for source type - fetch Roster
  
  # switch for source type - cleaning data
  
  # Split name
  x <- separateName(x)
  
  x <- joinStates(x)
}

# PrestoSports ------------------------------------------------------------


# WMT ---------------------------------------------------------------------


# Liberty -----------------------------------------------------------------



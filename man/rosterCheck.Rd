% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/googleUtils.R
\name{rosterCheck}
\alias{rosterCheck}
\title{Check for an existing roster in a Google Sheet and fetch/write if missing}
\usage{
rosterCheck(
  teamName,
  url,
  source,
  sport = c("baseball", "softball"),
  sheetid,
  overwrite = F
)
}
\arguments{
\item{teamName}{character. Name of the team / the sheet name within the Google Sheet.}

\item{url}{character. URL (or file path) used by getRoster() to fetch roster data.}

\item{source}{character. Source identifier passed to getRoster() to control how the roster is fetched. Currently supported for "sidearm", "wmt", "presto", "liberty", "espn".}

\item{sport}{character. One of "baseball" or "softball"; passed to getRoster() to select sport-specific parsing.}

\item{sheetid}{character. Google Sheet id or value acceptable to googlesheets4::read_sheet and googlesheets4::sheet_write.}

\item{overwrite}{logical(1). If TRUE, force fetching the roster and overwriting the sheet even if an existing roster is present. Default FALSE.}
}
\value{
A data.frame (tibble) containing the roster for the requested team after any processing by output_rosterVariables().
}
\description{
Attempts to read a roster for a given team from a Google Sheet. If the sheet read succeeds and contains more than one row (and overwrite is FALSE), the existing roster is returned. Otherwise the roster is fetched from the provided source via getRoster(), processed with output_rosterVariables(), and written back to the Google Sheet using googlesheets4::sheet_write().
}
\details{
This function first attempts a safe read of the sheet (using purrr::safely around googlesheets4::read_sheet).
If the read returns an error or the existing sheet is empty (or overwrite is TRUE), it will:
\itemize{
\item call getRoster(teamName, url, source, sport) to obtain raw roster data,
\item call output_rosterVariables() to produce a standardized roster data.frame,
\item write the resulting roster to the specified sheet with googlesheets4::sheet_write().
}
}
\examples{
\dontrun{
# Attempt to read "Team A" from Google Sheet and fetch if missing:
roster <- rosterCheck(
  teamName = "Team A",
  url = "https://example.com/roster.csv",
  source = "website",
  sport = "baseball",
  sheetid = "1A2b3C4d5E6fG7h8I9j0",
  overwrite = FALSE
)
}

}
\seealso{
getRoster, output_rosterVariables, googlesheets4::read_sheet, googlesheets4::sheet_write
}
